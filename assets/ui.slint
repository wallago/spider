// Build entry point. Slint generates one Rust module per compiled document, so
// every screen has to be reachable from this one file.
export { CreditScreen } from "credit_dashboard.slint";
export { StatusScreen } from "status_screen.slint";
